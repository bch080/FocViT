# FoveatedViT - Requirements

# Core dependencies (should be in autodl image)
torch>=1.9.0
torchvision>=0.10.0
numpy>=1.19.0

# Additional dependencies (need to install)
ml-collections>=0.1.0
tqdm>=4.62.0
scipy>=1.7.0
matplotlib>=3.3.0
Pillow>=8.0.0

# Optional (for distributed training / tensorboard)
# torch.distributed (built-in)
# tensorboard>=2.4.0
